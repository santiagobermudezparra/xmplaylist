[project]
name = "xm-spotify-sync"
version = "0.1.0"
requires-python = ">=3.13"

[tool.uv.workspace]
members = ["src/backend", "src/frontend"]

[dependency-groups]
dev = [
  "pytest>=8.3.0",
  "pytest-asyncio>=0.24.0",
  "pytest-cov>=6.0.0",
  "ruff>=0.8.0",
]
